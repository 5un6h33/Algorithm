#include<stdio.h>
#include<string.h>

#define TRUE 1
#define FALSE 0

//배열 기반 원형 큐



int main(void)
{
    int i;
    int n;
    scanf("%d", &n);

    char str[6];

    for(i = 0; i < n; i++)
    {
        scanf("%s", str);

        if(!strcmp(str, "push"))
        {

        }
    }

    return 0;
}